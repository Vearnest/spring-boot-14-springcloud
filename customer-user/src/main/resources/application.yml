server:
  port: 8200
spring:
  application:
    name: customer-user
eureka:
  instance:
    prefer-ip-address: true # Register service with ip address
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

